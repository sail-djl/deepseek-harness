# 包清单 — 全量索引

> 所有包版本 `0.1.1-rc.2`，npm scope `@deepseek-ai/dsh-*`

## 1. core/ — 核心脊柱

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-agent` | `ctx.agents` | Agent 接口、注册表、initiator scope、事件词汇表 | Service |
| `dsh-agent-loop` | `ctx.agentLoop` | Agent 驱动实现（LLM 调用 + 工具执行循环） | Plugin |
| `dsh-scope` | — | 作用域上下文注册原语 | Service |
| `dsh-session` | `ctx.sessions` | 事件溯源 session store（追加式日志） | Service |
| `dsh-tools` | `ctx.tools` | 工具注册表和执行管道（pre/guard/around/post） | Service |
| `dsh-system-prompt` | `ctx.systemPrompt` | System prompt 组装注册表 | Service |
| `dsh-agent-tool-presentation` | — | Agent 平面工具展示选择器 | Plugin |
| `dsh-agent-default-model` | `ctx.agentDefaultModel` | 默认模型选择 | Plugin |

## 2. llm/ — LLM 服务层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-llm` | `ctx.llm` | Provider-neutral LLM 适配器注册 + 流式调用 | Service |
| `dsh-llm-deepseek` | — | DeepSeek chat-completions SSE 适配器 | Plugin |
| `dsh-llm-pi-ai` | — | pi-ai backed DeepSeek 适配器 | Plugin |
| `dsh-llm-retry` | — | LLM 请求重试策略 | Plugin |
| `dsh-token-meter` | — | Token 用量计量 | Service |

## 3. fs/ — 文件系统层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-fs` | `ctx.fs` | 抽象文件系统接缝 | Service |
| `dsh-fs-local` | — | 本地文件系统实现 | Plugin |
| `dsh-fs-sandbox` | — | 沙箱强制文件系统实现 | Plugin |
| `dsh-fs-observation-policy` | — | 文件上下文策略（observed-state） | Plugin |
| `dsh-tool-fs` | — | 模型面向的文件系统工具（read/write/edit） | Plugin |
| `dsh-tool-fs-search` | — | 文件搜索工具（glob/grep） | Plugin |
| `dsh-tool-str-replace-editor` | — | 字符串替换编辑器 | Plugin |

## 4. shell/ — Shell 执行层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-shell` | `ctx.shell` | 抽象 bash executor 接缝 | Service |
| `dsh-bash-local` | — | 本地子进程 bash | Plugin |
| `dsh-pwsh-local` | — | 本地 PowerShell | Plugin |
| `dsh-bash-sandbox` | — | 沙箱 bash 执行器 | Plugin |
| `dsh-pwsh-sandbox` | — | 沙箱 PowerShell 执行器 | Plugin |
| `dsh-shell-env` | — | DSH_* shell 环境变量注册表 | Plugin |
| `dsh-tool-bash` | — | 模型面向的 bash 工具 | Plugin |
| `dsh-tool-pwsh` | — | 模型面向的 pwsh 工具 | Plugin |
| `dsh-tool-bash-persistent` | — | 持久化 owner-scoped bash 工具 | Plugin |
| `dsh-tool-pwsh-persistent` | — | 持久化 owner-scoped PowerShell 工具 | Plugin |

## 5. sandbox/ — 沙箱安全层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-sandbox` | `ctx.sandbox` | 抽象进程沙箱接缝 | Service |
| `dsh-sandbox-local` | — | 本地沙箱后端（bwrap/landlock/Seatbelt） | Plugin |
| `dsh-sandbox-policy` | — | 每次调用的沙箱策略解析器 | Plugin |

## 6. session/ — 会话持久化层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-session-persistence` | `ctx.sessionPersistence` | 抽象持久化接缝 | Service |
| `dsh-session-projection` | — | Session 投影接缝 | Service |
| `dsh-session-persistence-jsonl` | — | JSONL 持久化后端 | Plugin |
| `dsh-session-persistence-sqlite` | — | SQLite 持久化后端 | Plugin |
| `dsh-session-projection-cache` | — | 投影缓存层 | Plugin |
| `dsh-session-checkpoint-policy` | — | Session checkpoint 策略 | Plugin |
| `dsh-session-title` | — | Session 标题生成 | Plugin |
| `dsh-session-title-llm` | — | LLM-based session 标题 | Plugin |
| `dsh-session-title-first-prompt-llm` | — | 首条 prompt 标题 | Plugin |
| `dsh-session-title-all-prompts-llm` | — | 所有 prompt 标题 | Plugin |
| `dsh-session-telemetry` | — | Session 遥测 | Plugin |
| `dsh-session-telemetry-otel` | — | OpenTelemetry session 遥测 | Plugin |
| `dsh-session-stats` | — | Session 统计 | Plugin |

## 7. session-query/ — 会话查询层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-session-query` | Session 查询接缝 | Service |
| `dsh-session-query-sqlite` | SQLite session 查询后端 | Plugin |
| `dsh-session-log-export` | Session 日志导出 | Plugin |
| `dsh-tool-session-query` | 模型面向的 session 查询工具 | Plugin |

## 8. web/ — 网络访问层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-web` | `ctx.web` | 抽象 web 访问接缝 | Service |
| `dsh-web-fetch-http` | — | 匿名公共 HTTP(S) fetch | Plugin |
| `dsh-web-search-deepseek` | — | DeepSeek-backed 搜索 | Plugin |
| `dsh-tool-web` | — | 模型面向的 web 工具 | Plugin |

## 9. lsp/ — 语言服务层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-lsp` | `ctx.lsp` | 抽象 LSP 接缝 | Service |
| `dsh-lsp-stdio` | — | 通用 stdio language-server | Plugin |
| `dsh-tool-lsp` | — | 模型面向的 LSP 工具 | Plugin |

## 10. sdk/ — SDK 层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-sdk-client` | TypeScript 客户端 SDK（stdio JSON-RPC） | Plugin |
| `dsh-sdk-jsonrpc-server` | stdio JSON-RPC server | Plugin |
| `dsh-sdk-protocol` | 共享 wire protocol | Plugin |

## 11. mcp/ — MCP 协议层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-mcp-client` | MCP 客户端桥接（连接 MCP servers + 注册工具） | Plugin |

## 12. credentials/ — 凭证/授权层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-credentials` | `ctx.credentials` | 抽象凭证接缝 | Service |
| `dsh-credentials-local` | — | 基于文件的凭证 provider | Plugin |
| `dsh-authorization` | `ctx.authorization` | 授权接缝（人机对话获取凭证） | Service |

## 13. settings/ — 设置层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-settings` | `ctx.settings` | 抽象用户设置接缝 | Service |
| `dsh-settings-file` | — | 基于文件的 settings provider | Plugin |

## 14. storage/ — 存储层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-storage` | `ctx.storage` | 存储中心（命名后端注册表） | Service |
| `dsh-storage-domain` | — | 域数据表单（schema-validated KV） | Plugin |
| `dsh-storage-json` | — | JSON 存储后端 | Plugin |
| `dsh-storage-sqlite` | — | SQLite 存储后端 | Plugin |

## 15. compaction/ — 上下文压缩层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-compaction` | `ctx.compaction` | 抽象 compaction 接缝 | Service |
| `dsh-compaction-basic` | — | Token-meter 驱动 + LLM 摘要 | Plugin |
| `dsh-compaction-tool-result-pruner` | — | 工具结果修剪器 | Plugin |
| `dsh-command-compact` | — | /compact slash 命令 | Plugin |

## 16. spill/ — 溢出存储层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-spill` | `ctx.spillStore` | 抽象溢出存储接缝 | Service |
| `dsh-spill-local` | — | 本地文件系统溢出 | Plugin |
| `dsh-spill-policy` | — | 溢出策略 | Plugin |

## 17. goal/ — 目标管理层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-goal` | `ctx.goal` | 事件溯源同 session goal 状态 | Service |
| `dsh-tool-goal` | — | 模型面向的 goal 工具 | Plugin |
| `dsh-command-goal` | — | /goal slash 命令 | Plugin |
| `dsh-goal-round-driver` | — | Goal round 驱动器 | Plugin |

## 18. jobs/ — 后台作业层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-jobs` | `ctx.jobs` | 后台作业注册表 | Service |
| `dsh-jobs-local` | — | 本地作业后端 | Plugin |
| `dsh-tool-jobs` | — | 模型面向的作业工具 | Plugin |

## 19. skill/ — 技能层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-skill` | `ctx.skills` | Agent 技能 provider 注册表 | Service |
| `dsh-skill-filesystem` | — | 本地文件系统技能 provider（chokidar + yaml） | Plugin |
| `dsh-skill-badge` | — | 技能徽章 | Plugin |
| `dsh-tool-skill` | — | 模型面向的技能加载工具 | Plugin |

## 20. typert/ — 类型反射层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-typert-protocol` | Remote metadata + Typert provider protocols | Plugin |
| `dsh-typert-registry` | 运行时注册表（包反射 + Zod schemas） | Plugin |
| `dsh-typert-loader` | Typert package contributions 的 loader 集成 | Plugin |

## 21. context/ — 上下文注入层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-agent-instructions` | AGENTS.md/CLAUDE.md 指令文件加载器 | Plugin |
| `dsh-file-reference` | 文件引用发现合约和 @file grammar | Service |
| `dsh-file-reference-local` | 本地文件引用 provider | Plugin |
| `dsh-session-reference` | 跨 session 快照引用 | Plugin |
| `dsh-time-context` | 每步当前时间和耗时注入 | Plugin |
| `dsh-tmux-context` | tmux pane/window 位置注入 | Plugin |

## 22. code-runtime/ — 代码执行层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-code-runtime` | `ctx.codeRuntime` | 抽象代码执行接缝 | Service |
| `dsh-code-runtime-python` | — | CPython subprocess 实现 | Plugin |
| `dsh-code-runtime-worker-thread` | — | Worker thread 实现 | Plugin |

## 23. host/ — 宿主/服务器层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-host-apiproxy` | API 网关（ApiProxy 合约 + host-side 网关） | Plugin |
| `dsh-host-webserver` | Web 路由注册 | Plugin |
| `dsh-host-plugin-inventory` | Cordis Loader 插件状态只读投影 | Plugin |
| `dsh-host-frontend-static` | 前端静态资源 | Plugin |
| `dsh-host-directory-picker*` | 目录选择器（auto/browse/native） | Plugin |

## 24. api/ — API 网关层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-api-gateway` | Typert Remote Host dispatcher + Client API endpoint | Plugin |
| `dsh-api-remotes` | Remote BFF assembly + Host Agent/Session lookup | Plugin |

## 25. acp/ — Agent Client Protocol

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-acp` | ACP server（JSON-RPC stdio 驱动 agents） | Plugin |

## 26. e2b/ — E2B 云沙箱

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-e2b` | E2B sandbox lifecycle | Plugin |
| `dsh-fs-e2b` | E2B 文件系统实现 | Plugin |

## 27. interaction/ — 用户交互层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-commands` | — | 人类命令注册表 | Service |
| `dsh-user-approval` | `ctx.approval` | 用户审批接缝 | Service |
| `dsh-user-questions` | — | 用户问题接缝 | Plugin |
| `dsh-tool-ask-user` | — | 用户提问工具 | Plugin |
| `dsh-permission-presets` | — | 权限预设 | Plugin |

## 28. attachment/ — 附件层

| 包名 | ctx key | 职责 | 模式 |
|:--|:--|:--|:--|
| `dsh-attachment` | — | 耐久不可变附件存储接缝 | Service |
| `dsh-attachment-local` | — | 私有内容寻址 DSH_HOME 附件存储 | Plugin |

## 29. hooks/ — Hook 协议层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-hook-protocol` | Claude Code/Codex hook wire protocol | Plugin |
| `dsh-hooks-codex` | Codex hook 实现 | Plugin |
| `dsh-hooks-claude-code` | Claude Code hook 实现 | Plugin |

## 30. guard/ — 守护/策略层

| 包名 | 职责 | 模式 |
|:--|:--|:--|
| `dsh-tool-call-timeout-policy` | 工具调用超时策略 | Plugin |
| `dsh-repeat-tool-reminder` | 重复工具调用提醒 | Plugin |

## 31. boot/ — 启动引导层

| 包名 | 职责 | 说明 |
|:--|:--|:--|
| `dsh-app-boot` | 应用启动胶水（.env loading、Loader guards） | 依赖 cordis-plugin-group/loader/hmr |
| `dsh-cmdline` | 不可变命令行参数传递 | 依赖 cordis-plugin-loader |

## 32. bundle/ — 分发包聚合

| 包名 | 职责 | 说明 |
|:--|:--|:--|
| `dsh-base` | 核心 profile bundle | 70+ workspace deps 聚合 |
| `dsh-headless` | 一次性 bundle（无 Host/HTTP） | 依赖 commander |
| `dsh-web-app` | 浏览器端 bundle | 100+ workspace deps，最大聚合包 |

## 33. client/ — 前端客户端层

30+ 个 UI 包，包含：
- `dsh-client-web` — Web 入口
- `dsh-client-runtime` — 运行时
- `dsh-client-connection` — 连接管理
- `dsh-client-hmr` — 热更新
- `dsh-client-locale` — 国际化
- `dsh-client-ui-layout` — 布局
- `dsh-client-ui-conversation` — 对话
- `dsh-client-ui-sidebar` — 侧边栏
- `dsh-client-ui-theme` — 主题
- `dsh-client-ui-goal` — 目标 UI
- `dsh-client-ui-plan` — 计划 UI
- `dsh-client-ui-tool` — 工具 UI
- `dsh-client-ui-jobs` — 作业 UI
- 等

## 34. 其他包

| 分类 | 包名 | 职责 |
|:--|:--|:--|
| extensions | `dsh-tool-cordis` | 自引用 cordis 工具集 |
| extensions | `dsh-ui-cordis` | Cordis UI |
| extensions | `dsh-cordis-host-runner` | Host 端 cordis runner |
| extensions | `dsh-cordis-client-runner` | Client 端 cordis runner |
| experimental | `dsh-experimental-agent-team` | Agent Teams roster（private: true） |
| feedback | `dsh-message-feedback` | 每消息评分/笔记 sidecar |
| feedback | `dsh-command-feedback` | /feedback 命令 |
| identity | `dsh-anonymous-user-id` | 匿名用户身份 |
| plan | `dsh-plan-mode` | Plan mode（日志化、部署指导） |
| preset | `dsh-persona` | 部署 persona section |
| preset | `dsh-agent-presets` | Per-session agent composition |
| subagent | `dsh-subagent` | 子 agent 管理 |
| workspace | `dsh-workspace` | Workspace 实体注册表 |
| workflow | `dsh-workflow` | 工作流能力接缝 |
| terminal | `dsh-tool-terminal` | 终端工具 |
| todo | `dsh-tool-todo` | TODO 工具 |
