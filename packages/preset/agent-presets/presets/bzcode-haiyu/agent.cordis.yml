# The `bzcode-haiyu` agent preset: 百洲海渔项目开发者。
#
# 基于 bzcode，集成海渔相关 MCP Server。
# 包含：MySQL 海渔/酒店/秀屿/闽投/集控 + PostgreSQL + Redis + TDengine + SSH 海渔节点

# ── identity ────────────────────────────────────────────────────────────────

- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: >-
      You are a coding agent powered by the {{model}} model. Your working directory is {{cwd}}.
      You have access to 海渔项目相关的 MCP tools (MySQL, PostgreSQL, Redis, TDengine, SSH).

- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536

# ── shell ───────────────────────────────────────────────────────────────────

- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'

- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

# ── filesystem ──────────────────────────────────────────────────────────────

- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

- id: tool-fs-search
  name: '@deepseek-ai/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: false

# ── background jobs ────────────────────────────────────────────────────────

- id: tool-jobs
  name: '@deepseek-ai/dsh-tool-jobs'

# ── skills ──────────────────────────────────────────────────────────────────

- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'

# ── goals ───────────────────────────────────────────────────────────────────

- id: tool-goal
  name: '@deepseek-ai/dsh-tool-goal'

# ── plan mode ───────────────────────────────────────────────────────────────

- id: planning
  name: cordis:group
  group: true
  isolate:
    planMode: true
  config:
    - id: plan-mode
      name: '@deepseek-ai/dsh-plan-mode'
      config:
        section: |
              You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed to keep the tool catalog unchanged. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch mode manually; do not proceed with implementation.

# ── compaction ──────────────────────────────────────────────────────────────

- id: compaction
  name: cordis:group
  group: true
  isolate:
    compaction: true
    toolResultPruner: true
  config:
    - id: compaction-basic
      name: '@deepseek-ai/dsh-compaction-basic'

    - id: command-compact
      name: '@deepseek-ai/dsh-command-compact'

    - id: tool-result-pruner
      name: '@deepseek-ai/dsh-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

# ── delegation and workflows ────────────────────────────────────────────────

- id: delegation
  name: cordis:group
  group: true
  isolate:
    workflowEngine: true
  config:
    - id: tool-subagent-control
      name: '@deepseek-ai/dsh-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@deepseek-ai/dsh-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        backgroundMode: continuable

    - id: tool-subagent-fork
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: continuable

    - id: tool-subagent-codex
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: codex
        toolName: subagent_codex
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: tool-subagent-claude-code
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: claude-code
        toolName: subagent_claude_code
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: workflow-worker-thread
      name: '@deepseek-ai/dsh-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@deepseek-ai/dsh-tool-workflow'

    - id: tool-ralph
      name: '@deepseek-ai/dsh-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

# ── remaining model-facing rows ─────────────────────────────────────────────

- id: tool-ask-user
  name: '@deepseek-ai/dsh-tool-ask-user'

- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true

- id: tool-web
  name: '@deepseek-ai/dsh-tool-web'
  config:
    fetch: false
    searchTimeoutMs: 60000

# ── presentation ────────────────────────────────────────────────────────────

- id: tool-presentation
  name: '@deepseek-ai/dsh-agent-tool-presentation'
  config:
    mode: code

# ── MCP: 海渔 MySQL ────────────────────────────────────────────────────────

- id: mcp-mysql-haiyu-dev
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: mysql-haiyu-dev
    command: npx
    args: ['-y', 'mysql-mcp-database-server']
    env:
      MYSQL_DATABASE: smart_breeding_vessel_dev
      MYSQL_HOST: 192.168.1.30
      MYSQL_PASSWORD: mysql@Bzxxjs123
      MYSQL_PORT: '3306'
      MYSQL_USER: root
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-mysql-hotel
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: mysql-hotel
    command: npx
    args: ['-y', 'mysql-mcp-database-server']
    env:
      MYSQL_DATABASE: hotel_db
      MYSQL_HOST: 192.168.1.30
      MYSQL_PASSWORD: mysql@Bzxxjs123
      MYSQL_PORT: '3306'
      MYSQL_USER: root
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-mysql-xiuyu
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: mysql-xiuyu
    command: npx
    args: ['-y', 'mysql-mcp-database-server']
    env:
      MYSQL_DATABASE: smart_breeding_vessel_xiuyu_online
      MYSQL_HOST: 192.168.1.30
      MYSQL_PASSWORD: mysql@Bzxxjs123
      MYSQL_PORT: '3306'
      MYSQL_USER: root
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-mysql-mintou
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: mysql-mintou
    command: npx
    args: ['-y', 'mysql-mcp-database-server']
    env:
      MYSQL_DATABASE: smart_breeding_vessel
      MYSQL_HOST: 192.168.1.252
      MYSQL_PASSWORD: smart_breeding@bz
      MYSQL_PORT: '3306'
      MYSQL_USER: smart_breeding
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-mysql-sea-jk
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: mysql-sea-jk
    command: npx
    args: ['-y', 'mysql-mcp-database-server']
    env:
      MYSQL_DATABASE: platform_prod
      MYSQL_HOST: 192.168.20.2
      MYSQL_PASSWORD: sbv_dev@123
      MYSQL_PORT: '3306'
      MYSQL_USER: sbv
    toolCallTimeoutMs: 60000
    failOnStartupError: false

# ── MCP: 海渔 PostgreSQL ───────────────────────────────────────────────────

- id: mcp-postgres-claw
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: postgres-claw
    command: npx
    args: ['-y', '@modelcontextprotocol/server-postgres', 'postgresql://postgres:Pg2025Secure@192.168.31.150:5432/claw_server']
    toolCallTimeoutMs: 60000
    failOnStartupError: false

# ── MCP: 海渔 Redis ────────────────────────────────────────────────────────

- id: mcp-redis-haiyu
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: redis-haiyu
    command: npx
    args: ['-y', '@modelcontextprotocol/server-redis', 'redis://:bzxx@192.168.1.252:6379/2']
    toolCallTimeoutMs: 60000
    failOnStartupError: false

# ── MCP: 海渔 TDengine ─────────────────────────────────────────────────────

- id: mcp-tdengine-haiyu
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: tdengine-haiyu
    command: python
    args: ['-m', 'tdengine_mcp_server', '-th', '192.168.1.30', '-tp', '6041', '-tu', 'root', '-pwd', 'taosdata', '-db', 'demo', '-trans', 'stdio']
    toolCallTimeoutMs: 60000
    failOnStartupError: false

# ── MCP: 海渔 SSH ──────────────────────────────────────────────────────────

- id: mcp-ssh-hy-xiapu
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-hy-xiapu
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=192.168.1.252', '--port=22', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: Mtxp@123++
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-ssh-hy-xiuyu
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-hy-xiuyu
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=183.253.117.206', '--port=61012', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: Mtxy@123++
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-ssh-hy-mintou
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-hy-mintou
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=112.49.36.82', '--port=61012', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: Mtha@123++
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-ssh-hy-huian
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-hy-huian
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=112.5.22.231', '--port=61012', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: Bzha@123++
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-ssh-neiwang-98
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-neiwang-98
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=192.168.1.98', '--port=22', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: bzroot123123
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-ssh-neiwang-30
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-neiwang-30
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=192.168.1.30', '--port=22', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: bzxxjs_Linux@1.30
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-ssh-hy-jikong
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-hy-jikong
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=121.40.89.159', '--port=3378', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: Bzxx@2022!@
    toolCallTimeoutMs: 60000
    failOnStartupError: false

- id: mcp-ssh-jikong-1
  name: '@deepseek-ai/dsh-mcp-client'
  config:
    transport: stdio
    serverName: ssh-jikong-1
    command: npx
    args: ['-y', 'ssh-mcp', '--', '--host=192.168.20.2', '--port=22', '--user=root', '--timeout=60000', '--maxChars=none']
    env:
      SSH_MCP_PASSWORD: abc.123
    toolCallTimeoutMs: 60000
    failOnStartupError: false
